const $=id=>document.getElementById(id);let db=null,boreCount=0,projects=[];function init(){try{db=supabase.createClient(BOB_CONFIG.SUPABASE_URL,BOB_CONFIG.SUPABASE_PUBLISHABLE_KEY);$("cloudStatus").textContent="Cloud configured";$("cloudStatus").className="cloud online"}catch(e){$("cloudStatus").textContent="Cloud error"}}function safeName(n){return n.replace(/[^a-zA-Z0-9._-]/g,"_")}function current(){return JSON.parse(localStorage.getItem("bobCurrentDay")||"{}")}function saveCurrent(x){localStorage.setItem("bobCurrentDay",JSON.stringify(x))}async function uploadFile(bucket,path,file){const{error}=await db.storage.from(bucket).upload(path,file,{upsert:true,contentType:file.type||undefined});if(error)throw error;return path}
async function loadProjects(){const{data,error}=await db.from("projects").select("*").order("created_at",{ascending:false});if(error){$("projectList").textContent=error.message;return}projects=data||[];const s=$("projectSelect");s.innerHTML='<option value="">Choose project</option>';projects.forEach(p=>{const o=document.createElement("option");o.value=p.id;o.textContent=p.name;s.appendChild(o)});$("projectList").innerHTML=projects.map(p=>`<div class="project-item"><h3>${p.name}</h3><p>${p.contractor||""}</p>${p.dfn_pdf_path?'<span class="file-pill">DFN uploaded</span>':''}${p.production_template_path?'<span class="file-pill">Martin template uploaded</span>':''}</div>`).join("")||"<p>No projects yet.</p>"}
$("createProject").onclick=async()=>{const name=$("newProjectName").value.trim(),contractor=$("contractor").value.trim(),dfn=$("dfnFile").files[0],template=$("productionTemplate").files[0];if(!name||!dfn)return $("projectStatus").textContent="Project name and DFN PDF are required.";try{$("projectStatus").textContent="Creating project…";const{data:p,error}=await db.from("projects").insert({name,contractor,status:"active"}).select().single();if(error)throw error;const dfnPath=await uploadFile("project-files",`${p.id}/dfn/${safeName(dfn.name)}`,dfn);let templatePath=null;if(template)templatePath=await uploadFile("project-files",`${p.id}/production-template/${safeName(template.name)}`,template);const{error:u}=await db.from("projects").update({dfn_pdf_path:dfnPath,production_template_path:templatePath}).eq("id",p.id);if(u)throw u;$("projectStatus").textContent="Project and files saved to Bob’s cloud.";loadProjects()}catch(e){$("projectStatus").textContent=`Upload failed: ${e.message}`}};
function addBore(){boreCount++;const d=document.createElement("div");d.className="bore";d.innerHTML=`<div class="section-head"><h3>Bore ${boreCount}</h3><button class="small danger remove" type="button">Remove</button></div><div class="bore-grid"><label>Type<select class="type"><option>Ped to Ped</option><option>Ped to Stub</option><option>Stub to Ped</option><option>Stub to Stub</option><option>Road Crossing</option><option>Other</option></select></label><label>Footage<input class="ft" inputmode="numeric"></label></div><label>Description<input class="desc"></label>`;d.querySelector(".remove").onclick=()=>{d.remove();checkFootage()};d.querySelector(".ft").oninput=checkFootage;$("bores").appendChild(d);checkFootage()}function collectBores(){return[...document.querySelectorAll(".bore")].map(b=>({type:b.querySelector(".type").value,footage:Number(b.querySelector(".ft").value||0),description:b.querySelector(".desc").value.trim()}))}function checkFootage(){const t=Number($("totalFootage").value||0),s=collectBores().reduce((a,b)=>a+b.footage,0),e=$("footageCheck");if(!t&&!s){e.className="check neutral";e.textContent="Add bore footage to compare against the daily total."}else if(t===s){e.className="check ok";e.textContent=`Footage matches: ${t} ft.`}else{e.className="check warn";e.textContent=`Mismatch: daily total ${t} ft; bore total ${s} ft.`}}
$("startDay").onclick=()=>{const p=projects.find(x=>String(x.id)===String($("projectSelect").value));if(!p)return $("startStatus").textContent="Choose a project first.";saveCurrent({projectId:p.id,projectName:p.name,assignedPages:$("assignedPages").value.trim(),startedAt:new Date().toISOString(),status:"open"});$("startStatus").textContent="Day started and assignment saved."};async function uploadDayPhotos(key){const out=[];for(const[i,f]of[...$("dayPhotos").files].entries())out.push(await uploadFile("job-photos",`${key}/${Date.now()}-${i}-${safeName(f.name)}`,f));return out}
$("saveDay").onclick=async()=>{const c=current(),total=Number($("totalFootage").value||0),bs=collectBores();if(!c.projectId)return $("saveStatus").textContent="Start the day first.";if(total!==bs.reduce((a,b)=>a+b.footage,0))return $("saveStatus").textContent="Fix the footage mismatch before saving.";try{$("saveStatus").textContent="Saving…";const photos=await uploadDayPhotos(`${new Date().toISOString().slice(0,10)}-${crypto.randomUUID()}`);const row={project_id:c.projectId,Project:c.projectName,assigned_pages:c.assignedPages,actual_pages:$("actualPages").value.trim(),Total_footage:total,pedestals:Number($("pedestals").value||0),notes:JSON.stringify({bores:bs,pedestal_locations:$("pedLocations").value.trim(),general_notes:$("notes").value.trim(),photo_paths:photos}),photos:photos.length,production_sheet_complete:false,contractor_package_complete:false,redlines_complete:false};const{data,error}=await db.from("drilling_days").insert(row).select().single();if(error)throw error;saveCurrent({...c,...row,cloudId:data.id,status:"completed"});$("saveStatus").textContent="Completed day saved to Bob’s cloud."}catch(e){$("saveStatus").textContent=`Save failed: ${e.message}`}};
async function loadHistory(){const{data,error}=await db.from("drilling_days").select("*").order("created_at",{ascending:false}).limit(50);if(error)return $("historyList").textContent=error.message;$("historyList").innerHTML=(data||[]).map(x=>`<div class="history-item"><h3>${x.Project||"Project"}</h3><p>Assigned: ${x.assigned_pages||"—"} · Actual: ${x.actual_pages||"—"}</p><p>Footage: ${x.Total_footage||0} · Peds: ${x.pedestals||0}</p></div>`).join("")||"<p>No records yet.</p>"}
$("testCloud").onclick=async()=>{const{error}=await db.from("projects").select("id").limit(1);$("testResult").textContent=error?error.message:"Projects cloud connection works."};$("addBore").onclick=addBore;$("totalFootage").oninput=checkFootage;$("refreshProjects").onclick=loadProjects;$("refreshHistory").onclick=loadHistory;$("dayPhotos").onchange=()=>{const h=$("dayPhotoPreview");h.innerHTML="";[...$("dayPhotos").files].slice(0,12).forEach(f=>{const i=document.createElement("img");i.src=URL.createObjectURL(f);h.appendChild(i)})};document.querySelectorAll(".tab").forEach(t=>t.onclick=()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".panel").forEach(x=>x.classList.remove("active"));t.classList.add("active");$(t.dataset.tab).classList.add("active");if(t.dataset.tab==="projects")loadProjects();if(t.dataset.tab==="history")loadHistory()});init();addBore();loadProjects();if("serviceWorker"in navigator)navigator.serviceWorker.register("service-worker.js");
